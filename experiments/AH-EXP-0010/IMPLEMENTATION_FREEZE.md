# AH-EXP-0010 Implementation Freeze

## Status

**IMPLEMENTATION FROZEN BEFORE FINAL HOLDOUT**

This checkpoint records the implementation-fidelity review completed after exact-head CI #114 and before any AH-EXP-0010 final holdout outcomes are observed.

## Frozen implementation

- Source: `src/ahomeostasis/robust_counterfactual_abstention.py`
- Frozen source blob SHA: `e265801a0a4165742b5f0e85a48b681efc260881`
- Frozen PR head before this declaration-only commit: `88f3f45dcc19b7d6e4d13821a7030348dcd0d3ac`
- Exact-head PR CI: `#114` — passed
- Tests: `47 passed`

## Fidelity review

The implementation matches the preregistration on the following points:

- AH-EXP-0009 trajectory gates remain unchanged.
- Resource floor remains `0.40`.
- Backlog floor remains `3`.
- Counterfactual threshold remains `0.50`.
- AH-EXP-0009 counterfactual coefficients are reused unchanged through `counterfactual_advantage()`.
- Bounded disturbance views are `clip(d-0.08)`, `d`, and `clip(d+0.08)`.
- `robust_CF` is the minimum counterfactual score across the three bounded views.
- A successful intervention changes exactly one protective action to `NORMAL`; ordinary regulator control resumes on the next decision.
- Structural seed is `10010` with `256` schedules.
- Final holdout seed is `10011` with `512` schedules.
- The structural-only CLI contains no final-holdout execution path.
- CI invokes only `--structural-only` for AH-EXP-0010.

## Structural-validation evidence

CI #114 reported:

- structural candidates: `256`
- detector interventions: `58`
- mean interventions: `0.2265625`
- protective trajectories: `236`
- trajectories with NORMAL actions: `250`
- uncertainty width: `0.08`
- threshold: `0.50`
- `holdout_outcomes_observed = false`

These values are used only to establish that the implementation is live and structurally exercised. They are not used to tune any detector parameter or falsification criterion.

## Final-holdout constraint

The `10011` holdout remains unobserved at this checkpoint.

Before final evaluation, a separate evaluator may be added only if it:

1. uses this frozen detector source unchanged;
2. uses the preregistered 512 schedules from seed `10011`;
3. computes only preregistered aggregate and intervention-level measures;
4. is technically bound to one immutable commit (or an equivalent one-shot mechanism);
5. cannot re-execute merely because a branch receives a later push;
6. makes no change to detector gates, counterfactual coefficients, uncertainty width, threshold, generator constraints, utility definition, seed, or falsification criteria.

No AH-EXP-0010 final holdout result has been generated by this freeze commit.
